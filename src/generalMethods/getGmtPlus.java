package generalMethods;

public class getGmtPlus {
	
	/* I don't understand. Let's maybe try later.
	
	// A method to return a string stating the time after GMT alteration
	
	private static long totalMilliseconds, totalSeconds, currentSecond, totalMinutes,
	currentMinute, totalHours, currentHour;

	public static void main(String[] args) {
		// not used

	}


	public getGmtPlus(long gmtOffset){
		totalMilliseconds = System.currentTimeMillis();
		totalSeconds = totalMilliseconds / 1000;
		currentSecond = totalSeconds % 60;
		totalMinutes = totalSeconds / 60;
		currentMinute = totalMinutes % 60;
		totalHours = totalMinutes / 60;
		currentHour = totalHours % 24;
		currentHour += gmtOffset;
		return String.format("The current time is %02d:%02d:02d", currentHour, currentMinute, currentSecond);

	}
	
	*/

}
